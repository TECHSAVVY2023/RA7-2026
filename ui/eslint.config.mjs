// @ts-check
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt(
  // You can pass an array or object here for custom rule overrides
  {
    rules: {
      // Your custom rules go here if you want them later
    }
  }
)
